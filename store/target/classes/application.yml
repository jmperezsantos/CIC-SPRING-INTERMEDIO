spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/store_db
    username: root
    password: root
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect